//! The peripherals driver, delivers the "payloads" for certain predictions

/// Moves certain peripherals when the driver is distracted
pub fn deliver_distracted_payload() {
    todo!("Do peripheral stuff when distracted")
}
